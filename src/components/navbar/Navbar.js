import React from 'react';
import { Link } from 'react-router-dom';
import { NavbarContainer, NavbarWrapper } from './Navbar.style';

const Navbar = () => {
    return (
        <NavbarContainer>

        </NavbarContainer>
    );
};

export default Navbar;
